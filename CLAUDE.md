# CLAUDE.md — hardware-lab

我的硬件学习仓:每个子目录是一个独立的小项目,共享下面的工作约定。

## 子项目
- **[sleepy-sensor/](sleepy-sensor/)** — ESP32-C3「树莓派 Zero 形态」低功耗温度计 / 开发板,可接墨水屏 HAT。
- **[pisugar-clone/](pisugar-clone/)** — cleanroom 实现 PiSugar 3 I²C 协议的树莓派电源 / UPS HAT(协议兼容,非逆向)。

每个子项目的设计「源头」是它自己的 `<子项目>/docs/design.md` —— 决策/选型/理由都在那;改决策就改它并提交。

## 工作约定(所有项目通用)
- 用中文沟通。
- **推荐元器件/方案前必查官方数据手册**核实关键参数(电流能力、封装、Iq、压降);拿不准标 unclear,别凭记忆。
- **别把结论说过头**;区分「不能用」与「够用但不够稳健」,诚实给折中;被纠正直接认并修正(不必道歉)。
- 硬件在 EasyEDA Pro(云端);版本化用「文件→导出→工程(.epro)」放各子项目 `hardware/` 提交。
- 手焊为主(0805 起步);有加热台,可整板回流 QFN / 带散热盘封装。

## 经验教训(跨项目通用,选型务必记住)
- HT7533(只 100mA)漏查电流能力,扛不住 ESP32 WiFi 峰值 345mA。**选型必核电流。**
- **先抓串口看 `rst:` 原因再定性**:USB 连接/断开循环不一定是电源问题,也可能只是空片 ROM 循环(v1 实测踩过)。
- **临时排障改动必须记录回滚项**:口头「记得换回」会被忘(v1 的 R6 即此,遗留超规格隐患)。
- 曾把 charger-only 说成「必须装电池才能开机」被纠正:充电 IC 在 CV 模式能给系统供电,**无电池烧录多半可行**;power-path 的真正价值是「更稳健 + 榨干电池」,不是「否则开不了机」。
